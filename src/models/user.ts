export type UserData = {
    firstName:string,
    lastName:string,
    age:string,
}

export type UserAddress = {
    street:string,
    city:string,
    country:string,
    zip:string,
}

export type UserAccount = {
    email:string,
    password:string,
}